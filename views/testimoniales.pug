extends ./layout/index

block contenido
    main.container.mt-5 
        h1.text-center.mt-5 #{pagina}

        //- Mostrar los testimoniles
        //- p= JSON.stringify(testimoniales)

        blockquote.text-center Lee sobre nuestros clintes y sus experiencias
        include ./layout/bloque_testimoniales

        .row 
            .col-md-12
                h2.w-100.text-center.d-block Agregar un Testimonial

                //- p= JSON.stringify(errores)
                if(errores)
                    each error in errores 
                        .col.alert.alert-danger.text-center= error.mensaje

            .row.justify-content-center.col 
                .col-md-8 
                    form(action="/testimoniales" method="POST") 
                        .form-group 
                            label(for="nombre") Nombre 
                            input#nombre(type="text", name="nombre", placeholder="Tu Nombre", class="form-control" value= nombre) 
                        .form-group 
                            label(for="correo") Correo Electrónico 
                            input#correo(type="text", name="correo", placeholder="Tu Correo", class="form-control" value= correo) 
                        .form-group 
                            label(for="mensaje") Mensaje 
                            textarea#mensaje(name="mensaje", placeholder="Tu Mensaje" class="form-control", rows="3")= mensaje 

                        input.btn.btn-success.btn-block(type="submit")